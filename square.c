#include<stdio.h>
int square(int x){
    return x*x;
    printf("the square of number is %d",return);
}
int main(){
    int x;
    printf("enter the number");
    scanf("%d",&x);
    printf("the square of a number is %d",square(x));
    return 0;
}